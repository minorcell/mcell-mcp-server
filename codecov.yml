coverage:
  precision: 2
  round: down
  range: 0..100
  status:
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%

ignore:
  - 'tests/**'
